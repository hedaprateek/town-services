<?xml version="1.0" encoding="utf-8"?>
<!-- The window is painted the page's own background before the WebView has
     anything to show, so launching does not flash white. launch_bg and
     light_status are defined per flavour. -->
<resources>
  <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
    <item name="android:windowBackground">@color/launch_bg</item>
    <item name="android:statusBarColor">@color/launch_bg</item>
    <item name="android:navigationBarColor">@color/launch_bg</item>
  </style>
</resources>
